import React from "react"
import { DefaultButton } from "../../../../shared"

const StartAction = () => {
  return <DefaultButton text="Start analysing" link="/onboarding" />
}

export default StartAction
